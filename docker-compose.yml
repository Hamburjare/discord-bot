services:
  app:
    image: oven/bun
    container_name: discord-bot
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - .:/app
    working_dir: /app
    command: ["sh", "-c", "bun install && bun prod"]
